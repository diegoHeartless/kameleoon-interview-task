# Kameleoon Interview Task

Веб-приложение для визуализации данных A/B тестирования с интерактивным графиком конверсий.

## 📚 Выбранная библиотека визуализации

**Apache ECharts v6.0.0** — выбрана для построения графиков.

### Почему ECharts:
- 🎨 Мощные возможности кастомизации и настройки
- ⚡ Высокая производительность при работе с большими объемами данных
- 📱 Встроенная поддержка адаптивности и интерактивности
- 🛠️ Богатый набор встроенных функций (zoom, tooltip, toolbox)
- 📊 Поддержка различных типов графиков (линейные, сглаженные, с областями)
- 🌐 Отличная документация и активное сообщество

## ✨ Реализованные и бонусные функции

### Обязательные функции:
- ✅ **Отображение графика конверсий** — визуализация данных по вариантам тестирования
- ✅ **Выбор варианта (Variation)** — фильтрация по отдельному варианту или просмотр всех вариантов одновременно
- ✅ **Выбор стиля графика**:
  - Line — обычная линейная диаграмма
  - Smooth line — сглаженная кривая
  - Area — диаграмма с заливкой области

### Бонусные функции:
- 🌓 **Темная/светлая тема** — переключение между темами интерфейса
- 📅 **Zoom по неделям** — выбор недельного диапазона для детального просмотра данных
- 🔄 **Reset Zoom** — быстрое сброс масштаба графика к полному диапазону
- 💾 **Сохранение графика** — экспорт графика в PNG через встроенный toolbox ECharts
- 📱 **Адаптивность** — автоматическое изменение размера графика при изменении размера окна
- 🎯 **Интерактивный tooltip** — при наведении показывается детальная информация с сортировкой по значению
- 🔍 **Data Zoom (Inside)** — масштабирование графика колесиком мыши или жестами на тачпаде
- 📊 **Легенда графика** — отображение всех серий при выборе "All variations"
- 🎨 **Кастомный форматтер tooltip** — красивое отображение данных с цветовыми индикаторами

## 🚀 Локальная настройка

### Требования:
- Node.js 20.19+ или 22.12+
- npm или yarn

### Установка зависимостей:

```bash
npm install
```

### Запуск проекта:

#### Режим разработки:
```bash
npm run dev
```
Приложение будет доступно по адресу `http://localhost:5173`

#### Запуск с локальным сервером данных:
Для работы с локальным API сервером выполните в двух терминалах:

**Терминал 1 (сервер данных):**
```bash
npm run server
```
Сервер запустится на порту `3001` и будет предоставлять данные по адресу `http://localhost:3001/data`

**Терминал 2 (фронтенд):**
```bash
npm run dev
```

#### Использование статического JSON файла:
Если локальный сервер недоступен или для тестирования, приложение автоматически использует файл `public/data.json`.

Для принудительного использования JSON файла (даже в режиме разработки):
```bash
VITE_USE_JSON_DATA=true npm run dev
```

### Сборка проекта:

#### Обычная сборка:
```bash
npm run build
```
Результат будет в папке `dist/`

#### Сборка для GitHub Pages:
```bash
npm run build:gh-pages
```

#### Предпросмотр продакшн сборки:
```bash
npm run preview
```

### Проверка кода:

#### Линтинг:
```bash
npm run lint
```

#### Форматирование:
```bash
# Проверка форматирования
npm run format:check

# Автоматическое исправление форматирования
npm run format
```

## 📁 Структура проекта

```
src/
├── api/              # API слой (получение данных)
├── components/       # React компоненты
│   └── chart/       # Компоненты графика
├── model/           # TypeScript типы и модели данных
├── App.tsx          # Главный компонент приложения
└── main.tsx         # Точка входа
```

## 🛠️ Технологический стек

- **React 19.2.0** — UI библиотека
- **TypeScript 5.9.3** — типизация
- **Vite 7.2.2** — сборщик и dev сервер
- **Apache ECharts 6.0.0** — библиотека визуализации
- **TanStack Query 5.90.8** — управление состоянием сервера
- **React Select 5.10.2** — компоненты выбора
- **ESLint** — линтинг кода
- **Prettier** — форматирование кода

## 📦 Деплой на GitHub Pages

Проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

### Автоматический деплой

При каждом push в ветки `main` или `master` проект автоматически собирается и деплоится на GitHub Pages.

### Настройка GitHub Pages

После первого push в `main`/`master` ветку workflow автоматически создаст ветку `gh-pages` и задеплоит проект. Затем нужно включить Pages в настройках репозитория:

1. В настройках репозитория GitHub перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите ветку `gh-pages`
3. В папке выберите `/ (root)`
4. Нажмите **Save**

После этого ваш сайт будет доступен по адресу: `https://<your-username>.github.io/kameleoon-interview-task/`

**Примечание**: Если ветка `gh-pages` еще не создана, подождите, пока workflow завершит деплой после первого push.

### Примечание о названии репозитория

Убедитесь, что название репозитория в `vite.config.ts` совпадает с реальным названием вашего репозитория на GitHub. Если репозиторий называется иначе, обновите строку `base` в `vite.config.ts`.

## 📝 Дополнительная информация

### Данные

Проект автоматически использует данные:
- **В режиме разработки**: Пытается подключиться к локальному серверу (`http://localhost:3001/data`), если он запущен
- **На GitHub Pages**: Использует статический JSON файл `public/data.json` (данные идентичны серверу)

### Особенности реализации

- ✅ Полная типизация TypeScript с JSDoc комментариями
- ✅ Строгие правила ESLint (включая проверку зависимостей хуков)
- ✅ Оптимизация производительности через `useMemo` и `useCallback`
- ✅ Адаптивный дизайн с поддержкой разных размеров экрана
- ✅ Обработка ошибок и состояний загрузки
